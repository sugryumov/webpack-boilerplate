import { IconCSS, IconJS, IconWebpack } from '../SVGIcon';
import './index.css';

export const App = () => (
  <div className="app">
    <div className="container">
      <IconJS />
      <IconCSS />
      <IconWebpack />
    </div>
  </div>
);
